Demo 4 & presentation planning meeting

Kyle docker demo:
	- two stage build process
	- ensures we are using JDK 23

	docker compose up --build


Action items:
	- how did the last demo go?
		- W

	- the docker container situation
		- what can we do to fix it?

	- demo 4: fault tolerance:
		- how do we detect a failed server?
			- suspicion of failure route
				- chat servers could use a timer to re-register if they sit dormant long enough



		- how do we establish a static IP? -> chloe
			- DNS routing
			- Kyle's range of valid IP idea
			- static vs. dynamic ports
			- subnet force take over port (limited, last resort)
			- duck DNS	

		- how do clients handle failures?
			- when a client fails?
		On reboot:
			1) re-enter log in information & re-authenticate with the addressing server
			2) be assigned to a chat server
			3) get whole chat log from chat server on reconnection

			- when a server the client is connected to fails?
			Timeout mechanism
			1) tell addressing server about the lost server

			ALREADY IMPLEMENTED (maybe?) THANKS KYLE

		- how do chat servers handle failures?
			- when a connected client fails?
			1) tell addressing server that a client has disconnected
			2) update own list of clients

			- when a neighboring server fails?
		Suspicion of failure via timeout? lack of messages
			1) tell addressing server 
			2) update own log of the network/existing server
			3) tell neighboring servers about the failure so they can also update their own network/server logs
		
			- when the addressing server fails? (and this server detects it)
		Chat servers should more regularly talk to the addressing server -> periodic pull requests for updated network topology
			1) wait?

		Alt route: chat servers are given logs of all backups, could try and contact a replica about the failure
		- backup server log is given at the time a server is registered AND/OR when a new addressing server is elected

		- how does the addressing server handle failures?
			- when a client fails?
			Does nothing
			Needs to be told by a chat server that the chat server has one less connection; update the number of connections for said server

			- when a server in the cluster fails?
			Needs to update its list of chat servers to prune out the dead server
		Other chat servers would need to pull network updates to be told about this.

			- when the primary address server fails?
		Option 1: timeout/missed ping option: backups routinely check with the primary
			- if the primary has crashed then this is detected via some method (timeout, missed pings, something) and an election is called
			- if the primary hasn't crashed (is just slow) then the cost of the election is considered to be acceptable even though the existing primary will win

		Option 2: Rely on the chat servers/clients to tell the backups about the crash
			- leader election is immediately called?
			- leans towards the backup server log book being kept by each chat server

			- when one of the backup address server(s) fails?
		Can this even be detected? By who? When? How?
		Would need to tell the other servers (chat and addressing) so they can update their log books
		- will be very rare, maybe a rare ack message? 






	- demo 4: leader election?
		- where are we doing for leader election
			addressing server: still using the bully election system we outlined in the original proposal 
		- task: write the bully protocol (open to anyone)
			
	- presentation
		- its happening

Outcomes/changes:
	- on initialization address servers run leader election (maybe on random numbers rather than process ID). After this initial election the primary undergoes the normal setup (including setting everyone's process IDs) 
		- maybe not possible due to the requirement of a static IP; we have to assume there is a primary server always (primary server is required to be 24/7)


	- backup/replica addressing server log book that will be shared with chat servers
		- chat servers will tell ALL entries in this log book about a failure -> redundancy

	- each socket connection (Client-Server and Server-Server) should have some kind of timeout mechanism; assumes a failure if no response after X time

	- each chat server has an "Am I Dead?" timer: if X time passes with no communication of any kind the chat server will re-register with the address server

	- when a chat server re-enters the network it performs "Synch from scratch"
		- maybe should do something to get a new copy of the chat log from its neighbors (some kind of "Hello" gossip message?) before handling new clients (ALREADY DONE??)

	


Goals this week:
Aidan:
	- working on getting address server replication working
	- will look into 
		- replica address book
		- detecting failure of a backup address server

Chloe:
	- client part of failure/communication protocol in general
	- will look into static IP problem (talk to Aidan)

Cole:
	- bully election protocol for the address server 
	- maybe client part of the failure detection protocol


Kyle:
	- continued working on user authentication/token passing for the login procedure 
	- Will need to talk to everyone basically

Parmeet:
	- switch the server stuff to the NIO socket
	- communication with client
	- add timeouts