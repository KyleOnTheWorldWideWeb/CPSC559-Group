Group 16 March 13 meeting: Demo 2: replication

Introduction/review:
What were people's thoughts on how demo 1 went? What can we do to improve this time?
	- Kyle: "i feel validated" 


What didn't get finished last time?
	- client-server communication 

--------
Goal for the this next demo: 
	At least 3 replicas of each type (clients and servers)

Main work task this week will be server-to-server messaging, basic message/chatlog replication tasks(doesn't need full synchronization), and registering chat servers with the address server. 

To achieve this we will likely need to work on:
	- How will the address server determine available chat servers? How does it determine which chat server to send a client to?
	- Server-to-server message passing
	- How will chatlogs be stored? How can they be looked up (ie: if i know i want to chat with Cole how do i/the server know where to route the messages to get them to Cole?) 

Additional/stretch work tasks would include:
	- Encryption/authentication tasks (Kyle's ticket system)
	- Implementing basic fault tolerance/leader election for the address server/gate or "primary" chat server 
	- Introducing persistent chat messages/ some way to look up chat(s)


Other topics of discussion:
	- presentation schedule 
	- Who will be available to work on this demo/the design doc and when?


-------
What we talked about
-------
- change sockets over to NIO sockets (event-based sockets) (middle to low priority?)
		- only changing over ChatServer sockets; [Aidan will do this]

- chat protocol (client-server communication): (high priority) 
	- how are objects (messages) passed between the two? 
	- time stamps

- server-server communication (high priority, multiple parts)
	- How are messages sent between server nodes?
		- when are time stamps (TS) applied? When it enters the chat server cluster (on message receive) 
		- what are time stamps: server ID + local-server time
			- logical vs real time
		- how are messages in the chat in the chat ordered? based on time stamps

	- server clusters can use Lamports timestamps/vector time stamps 

	- what kind of messages need to be passed?
		- Client -> Server: Chat Message (payload includes a message for the chat log)
		- Client -> Server: Chat Request
		- Server -> Client: Chat Message(s) (???)
		- Server -> Server: Chat Update ("Hey i received this message; please update your chat log"
		- Server -> Server: synchronization message
		- Server -> Server: Life check
		- Address Server -> Chat server : Life Check
		- Chat Server -> Address Server: Life check
		- Chat Server -> Address Server: Server lost update ("Hey, server X isn't responding")
		- Chat Server -> Address Server: Client lost update ("Hey, client X DNE")
		- 

- Client: how do you decide which chatroom/user your communicating with?
	- are we even doing multiple chat rooms? Kyle votes that we dont 
	- one big mega chat room

- How will chatlogs work on the coding side?
	- Aidan: database
		- each chat server has its own database
		- messages in a chatroom/chatlog identified by some ID value
		- order of messages determined by some value in the DB
	This is a future idea but we wont be implementing this this week (unless someone goes crazy)
	- For now ChatServer's have a local object ChatLog that is (maybe?) one large vector of messages (message + TS + sender + receiver)

-  Addressing server handling multiple chat servers in the network: (already done)
	- Register chat server with the addressing server  
	- Aidan claims this is already implemented; doesn't need be changed for now
	- Addressing server shares the ChatServer log with new ChatServers
		- some update mechanism for this log so that all servers in the network will have up-to-date copies of the server log

---
Meeting outcomes:
---
	- Will meet Monday for the demo + design doc work (time TBA) (probably 2-5)

	- Aidan: Docker compose update; network facing containers
			- stretch goal: figure out how ChatServer databases would work

	- Chloe: Server-Server communication 
					- figure out synchronization mechanism we are using (Lamport's vector time stamps)
					- 

	- Cole: Client-Server protocol from client perspective (using classic java sockets, not NIO)
		
	- Kyle: Server-Server communication
			- Server-Server communication what messages we need?

			- Channel: Server-to-Server
			- session token(s)/authentication (user persistent) 

	- Parmeet: 
			- initializing the chatlog object for ChatServers (persistent chatlog)
			- implementing Client-Server message passing from the Server's perspective
			-  work alongside Cole on this